# Login module

Place the login module specification / prompt content here.
When you send the login MD file, it will be used to implement `resources/keywords/login_keywords.resource`, `resources/locators/login_locators.resource`, and `tests/login_tests.robot`.
